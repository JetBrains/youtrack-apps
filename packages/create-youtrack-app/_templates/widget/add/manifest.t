---
to: manifest.json
inject: true
after: '\"widgets\": \['
# Injection is actually done by `inject-manifest.js`. This file is left just for nicer console output, so `inject: manifest.json` is printed
---
